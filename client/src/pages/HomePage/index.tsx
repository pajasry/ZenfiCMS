import { Template } from "@/components/layout";
import { Editor } from "@/Editor";

/**
 * Home page
 */
export const HomePage = () => {
    return (
        <Template>
            <Editor />
        </Template>
    );
};
