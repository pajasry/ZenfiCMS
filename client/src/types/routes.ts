export enum RoutesName {
    LOGIN = "/admin/login",
    HOME = "/admin",
    MEDIA = "/admin/media",
    POSTS = "/admin/posts",
    PAGES = "/admin/pages",
    APPEARANCE = "/admin/appearance",
    USERS = "/admin/users",
    SETTINGS = "/admin/settings",
}
